/*
#include <stdio.h>  

extern "C" {
#include "lua.h"  
#include "lualib.h"  
#include "lauxlib.h"  
}
lua_State* L;

int main(int argc, char *argv[])
{
	L = lua_open();  
	luaL_openlibs(L);
	luaL_dofile(L, "test.lua");
	lua_close(L);
	return 0;
}
*/

int main(int argc, char *argv[])
{
	return 0;
}